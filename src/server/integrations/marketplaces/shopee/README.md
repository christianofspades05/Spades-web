# integrations/marketplaces/shopee

Not implemented yet.

Same shape as `integrations/marketplaces/tiktok-shop`: OAuth connect flow
into `marketplace_connections`, order import into `orders`
(`source = 'shopee'`), SKU mapping via `marketplace_product_mappings`, and a
webhook receiver under `server/webhooks`.
