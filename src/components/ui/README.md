# components/ui

Shared, presentation-only primitives (Button, Input, Card, etc.) used across
storefront, account, checkout, and admin. Not built yet — add primitives
here as real features need them, rather than pre-building a component
library.
